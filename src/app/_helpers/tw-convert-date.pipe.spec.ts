import { TwConvertDatePipe } from './tw-convert-date.pipe';

describe('TwConvertDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TwConvertDatePipe();
    expect(pipe).toBeTruthy();
  });
});
